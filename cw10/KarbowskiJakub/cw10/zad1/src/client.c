#include "client.h"

int client_connect(client_session_t *session, const char *name, connection_type_t connection_type, const char *address)
{
    return 0;
}

int client_get_game(client_session_t *session)
{
    return 0;
}

int client_send_move(client_session_t *session, pos_t pos)
{
    return 0;
}
